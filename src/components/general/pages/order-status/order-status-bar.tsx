import { Order } from '@prisma/client'
import { Progress } from '@/components/ui/progress'

export default function OrderStatusBar({ order }: { order: Order }) {
  return <div>Order Status Bar</div>
}
